#
# Special Forms
#   (def name meta... value)
#   (var name meta... value)
#   (fn name? args body...)
#   (do body...)
#   (if condition when-true when-false?)
#   (quote x)
#   (splice x)
#   (while condition body...)
#   (break value?)
#   (set l-value r-value)
#
# Functions
#   mapcat
#
# Macros
#   defn
#   when
#
# Variables
#   default-peg-grammar
#
